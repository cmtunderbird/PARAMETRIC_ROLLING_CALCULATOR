fix: add /api/cmems/health so serverAlive() passes through Vite proxy

Path mismatch: serverAlive() fetches /api/cmems/health which Vite proxies
to localhost:5174/api/cmems/health, but cmems-server.js only had /api/health.
Express returned 404, serverAlive() returned false, test showed server-not-running
error even though the server was actually running (visible in the screenshot).

Fix: register both /api/health and /api/cmems/health on the Express server.
