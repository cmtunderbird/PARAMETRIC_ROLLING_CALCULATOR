fix: disable Vite auto-open, launch.bat is the single browser opener

vite.config.js: open changed from true to false.
Vite was opening one tab, launch.bat was opening another = two tabs at localhost:3000.
Now only launch.bat opens the browser, exactly once, after detecting the port.
