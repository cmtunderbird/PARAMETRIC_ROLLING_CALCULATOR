fix: Electron production paths + cmems_worker.py resolution

electron/main.js:
  - Added isPacked flag for packaged vs dev path resolution
  - app.setAppUserModelId for proper Windows taskbar grouping
  - CMEMS server spawned with CMEMS_WORKER_PATH env var so
    cmems-server.js finds cmems_worker.py in both dev and packaged builds
  - Added NODE_PATH env for bundled node_modules in packaged app
  - mainWindow.setMenuBarVisibility(false) for professional appearance
  - Icon path resolves from process.resourcesPath when packaged
  - BrowserWindow size 1440x920, minWidth 1100

cmems-server.js:
  - spawnWorker() respects CMEMS_WORKER_PATH env var (set by Electron main)
  - Falls back to __dirname-relative path for dev/standalone use

launch.bat:
  - Simplified to single Electron launcher
  - Auto-builds dist/ if missing (first run)
  - No more Vite dev server or browser needed

Desktop shortcut updated to launch.bat directly.
Tested: Electron opens, CMEMS server starts on 5174, Python worker ready.
